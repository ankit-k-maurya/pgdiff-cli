{
  "name": "pgdiff",
  "version": "0.1.0",
  "private": true,
  "description": "Diff two live PostgreSQL schemas and emit reversible up/down migrations.",
  "type": "module",
  "workspaces": [
    "packages/core"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w @pgdiff/core"
  }
}
